# 2026-09-13 手动测试失败诊断与修复

工作区仍为 `.worktrees/production-integration`。本次修复与之前的续作成果统一保存在集成分支，未合并主仓。

用户在原 Trip 的已有会话提交东京两天、当地预算 1500 元、文化/美食/散步需求。
该轮 request `req-q`，从 01:45:22 UTC 到 01:46:01 UTC，最终
`stop_reason=max_tool_steps`，没有新增攻略。前两次研究发生在更新 Trip 条件后、
恢复 Goal 新版本之前；随后 Qwen 原生研究连续三次 HTTP 429。DeepSeek 的调用
均 HTTP 200。三次错误回执没有更具体原因或 Retry-After，不能断言是账户额度、
并发还是供应商容量不足。证据是忽略目录中的第二轮账本、38/40/42 回执、
数据库 conversation metadata 与 research_generation_audits。

修复：

- 原生研究 429 明确分类为 PROVIDER_RATE_LIMITED；当前实例按 Retry-After
  或默认 30 秒冷却，两个研究工具共享，无额外调用/预留。首个失败审计和未知费用保留。
- Planner 获得可恢复的原因提示，更新条件后按当前版本激活 Goal；不增加步数或放宽交付校验。
- API 回复、持久消息及 workspace 恢复保留安全失败原因。未完成时显示限流说明，
  后来确实完成的 delivery 不被之前的 warning 覆盖。
- 新版规划显示“本次规划未完成”和原样重试；无结果时不显示空攻略说明与结果来源。

验证：后端定向 4 suites / 57 tests，PostgreSQL workspace 7 tests，后端类型检查；
前端展示行为 15、客户端恢复 62、UI 类型检查均通过。数据库测试使用独立 schema。
本地 API 已重启加载补丁，Vite HMR 已实际显示新的失败/重试状态。

修复后的真实请求 `req-3` 已结束：在页面点击“重试这次规划”，原始输入不变。
一次 Qwen 请求仍 HTTP 429；没有再次请求研究服务，随后 Planner 尝试复用旧研究。
旧研究与当前 Trip 版本不兼容，保存被拒绝，没有生成新攻略。最终仍到达工具步数
上限，但回复明确为“联网研究服务暂时限流，本轮未能完成攻略”，并持久保存
`research_provider_rate_limited` 与领域错误码。没有将此次重试记为生成成功。

本次同轮保护及前端诊断已生效，但 Planner 仍会消耗多步尝试不兼容的旧研究；
该低效恢复策略是后续优化项。实例冷却不跨 HTTP 请求。外部 429 未解除，
本轮不自动切模型、不循环重试、不重置未知费用。第二轮请求 45–56 均已收尾，
证据 `backend/.demo/rate-limit-fix-evidence.json`，新增一次未知研究预留 $0.50。
本地服务继续运行：API 3011（PID 52748）、Vite 4179、PG 15432。

后续耗时核对：原始用户请求约 39.4 秒，其中主模型 11 次共 34.0 秒，
研究 3 次共 4.6 秒；修复后重试约 279.7 秒，其中主模型 11 次共 265.8 秒，
研究 1 次共 12.6 秒。时间来自服务端消息与请求回执，不包含前端额外轮询延迟。
主模型等待包含网络/供应商等待/输出生成，没有首 token 计时，不能进一步拆分。
