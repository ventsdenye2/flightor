# G1 缺陷修复与真实快照复验

2026-09-20。基线 `861f857`，本批修复后的工作区。**错误内容现已被领域门槛拦截，G1 整体仍不放行**。本批没有运行 Planner 或实时 Provider，没有刷新来源、改写旧攻略或修改业务数据库；不能将复验称为新一轮线上成功或性能提升。

## 修复范围

- 研究工具复用服务器当前 Trip 内已解析的 canonical 地点，继续拒绝模型自行提供的未知地点事实；取消、owner、Trip/version 与保存约束保留。
- Research v2 finding 可选携带 `temporalEvidence`，来源原文中的完整 ISO 日期经服务器校验后才被采用。查询窗口、缓存过期时间、模糊季节及模型自写日期不构成活动发生时间证据。
- 共享攻略保存/持久完成校验器拒绝缺日期证据或日期不匹配的 event；把 event 放入 supportingEvidence 也不能绕过。无效可选日期证据只被丢弃，不使整批其他有效 finding 降级。边界和兼容见 [ADR 0020](../../adr/0020-canonical-trip-locations-and-temporal-evidence.md)。

## 本批实际验证

| 检查 | 结果 | 本次记录耗时 |
| --- | --- | --- |
| 后端类型检查与 build | 通过 | 未单独计时，不估算 |
| 完整离线后端 | 95 文件、745 项通过 | Vitest 79.75 秒 |
| 完整 PostgreSQL | 7 文件、37 项通过，无 skip | Vitest 23.76 秒 |
| 原始 SQL 恢复 | 成功；仅本批临时 PostgreSQL 16 | 1.523 秒，含 pg_isready 进程检查 |
| 自备机票数据加载 / 领域验证 | 原 satisfied → 当前 failed | 0.100 / 0.212 秒 |
| 已采用航班数据加载 / 领域验证 | 当前仍 satisfied | 0.075 / 0.170 秒 |
| 两例快照复验总耗时 | 含恢复、读取及验证 | 2.082 秒 |

恢复与验证数据见 [机器可读记录](G1_REPAIR_2026-09-20.metrics.json)。这是单次领域复验墙钟，非稳定延迟基准；两个测试套件耗时不包含此前开发、失败修正或人工审查时间。未把未计时阶段补成零或倒推出耗时。

原始输入为 `backend/.demo/g1-live-2026-09-20T12-12-39-743Z/database.sql`，保留 [原 live 报告](G1_LIVE_2026-09-20.md) 的全部历史结果。专用容器 `flightor-g1-repair-20260920` 使用 loopback 随机端口、tmpfs、自动移除；数据库为 `flightor_g1_live`。仅在该容器恢复 SQL，使用当前 build 的 owner-scoped Trip/Artifact/Goal/Run/Workspace repositories 读取原 Goal、最近 Run、当前 Trip 和 selectedFlight，再调用 `createDefaultGoalVerifierRegistry().verify`；脚本禁用全局 fetch。原始数据库数据不被 verifier 更新。执行辅助脚本留在忽略的 `backend/.demo/revalidate-g1.mjs`。

- 自备机票原攻略 `01a0bebf-3ed3-775c-b763-3f035a1fdb16`：现在返回 `guide_event_date_evidence_missing`、`guide_research_type:event`，不能再次判 satisfied。没有为通过验证删除其既有 event 必需约束。
- 已采用航班原攻略 `01a0bec2-6026-7015-8444-0c0087e4ce02`：仍 satisfied，保留粗时间段与部分核实证据的 warnings。该结果只证明当前领域约束通过，不抹去原报告中的文字质量边界。

## 剩余问题与下一步

1. Goal 的 `researchTypes` 当前仍同时承载研究范围与必需覆盖；模型自行追加类别导致返工的问题尚未修复。下一步需分离用户要求的交付约束与可探索研究范围，明确接受/兼容语义；不能静默删掉已接受目标要求或按用户文本关键词打补丁。
2. 初始时间证据仅支持源文完整 ISO 日期，其他自然语言日期保持未知；native research 当前不产该字段。该门槛证明源文绑定与日期抄录一致，不保证句子语义、分类和全部事实正确，也不涵盖实时营业时间或交通时刻证明。
3. 修复后 Planner/Provider 两条 live 链路、真实平台展示及正式 A/B 尚未重跑。旧准入账本占用仍为 US$0.517287 / US$2，本批新增付费请求为 0；不可通过新建运行目录重置同批额度与次数限制。
4. 当前业务 API/Worker 未重启部署。本批临时数据库验收后已核对名称/任务标签并停止，自动移除；不以源码/build 完成声称运行容器已更新。

文档工作区检查通过：74 份 Markdown、306 个相对链接；`git diff --check` 通过。提交前另核对实际暂存批次，排除原有 `docs/demo/DEMO_MASTER.md`。
