# 终稿生成实现与验证：2026-09-22

## 交付与边界

已实现发布末端单次编辑调用、zh/en 请求合同、来源/活动绑定、草稿隐藏、持久语言缓存和独立调用观测。字段/版本/扩展接口由 [ADR 0025](../../adr/0025-bounded-guide-finalization.md) 维护。当前主 Planner 模型、研究 Provider、工具选择、Goal/Run与航班业务未重构。未重启 Docker、数据库或微信 SDK，也未部署/重启正在运行的 API。

本轮原有未提交的微信验收、研究查询和 Planner 修正保留，提交只纳入终稿增量。旧费用账本未修改。真实模型只在用户追加 US$2 授权后运行，输入为留存完整研究 fixture，搜索调用为0。

## 实际验证

| 检查 | 结果/范围 |
| --- | --- |
| 后端完整离线回归 | 最终103文件、826项通过（109.93秒）；先前102文件825项是中间批次，不累加 |
| 终稿及接点定向 | 全量回归后补全占位正文防护，最终17项通过：zh/en、中文输入英文locale、原文名称、内部叙述、双语重复、预算/精确声明、全占位正文、无效JSON、唯一修复、缺材料/practical主活动、语义冲突、超时、取消、去重、持久恢复、版本/owner检查；CloudPlanner实际保存隐藏草稿、复用同model/client、普通聊天不再编辑 |
| PostgreSQL | 自备票/已选航班2案例通过；真实短事务合并双语言，独立连接恢复、错hash拒绝、owner隔离、Trip变更拒绝，原始days及版本不变。既有服务器只开放5432，使用postgres库随机隔离schema并清理，未操作flightor业务记录。首轮测试owner fixture错误使用非bigint，修正为合法非owner id后复验通过 |
| 前端 | 新增语言缓存/迟到/owner4项，conversation-progress24项、进度组件6项、publication组件9项、Plan边界12项、telemetry8项通过；Artifact25项、production presentation28项、reply6项、library7项、session recovery20项通过；Phase5旧请求精确字段断言补locale后86项通过，Phase6 22项、Phase7–9 10项通过 |
| 类型与构建 | backend check/build、前端tsc通过；微信构建退出0，保留既有CSS顺序及bundle大小警告，不宣称真机验证；新增前端语言测试纳入test:artifacts |
| 未测 | 本轮未运行真实微信端到端发起/切换/刷新，没有对生产环境部署；没有跨进程去重、崩溃恢复或完整质量基准验收 |

## 真实模型与成本（包含失败）

实际模型 `deepseek/deepseek-v4-flash-0731`，client为当前OpenRouterClient，reasoning关闭；同源材料、无工具、无搜索。完整结果在 [中英示例JSON](FINALIZATION_EXAMPLES_2026-09-22.json)，包含中文初稿、英文初稿和基于接纳中文的英文翻译；活动身份、顺序和来源关系相同。接纳是本轮程序合同与模型编辑结果，不构成独立事实核验或全部G1放行。

| 最后烟测 | 调用数 | 耗时 | prompt/completion token | 已知费用（USD，向上取微美元） |
| --- | --- | --- | --- | --- |
| 中文初稿 | 1 | 28.375秒 | 7993 / 894 | 0.001712 |
| 中文需求、英文界面初稿 | 1 | 19.831秒 | 8659 / 1018 | 0.000889 |
| 已接纳中文→英文 | 1 | 40.271秒 | 1435 / 950 | 0.000772 |

总账本9次模型出站，已知费用 **US$0.006764**，2次超时未知费用继续预留 **US$0.20**，累计占用 **US$0.206764 / US$2**。所有原账本hash保持一致。未做跨案例速度比较。

失败记录没有删除：初版12k输出被旧计费器拒绝，0出站；随后45秒总窗口内中/英各初始+修复，均超时；扩大共同窗口至90秒后中文在第二次输出仍因sourceRefs格式失败。诊断发现模型将JSON pair字符串改成单引号数组/斜杠形式；合同改为无嵌套转义的 `artifact-id/finding-id`，仍做完全匹配，最后三例各一次调用接纳。源材料没有被删除，未放松来源校验。公开表达补充拒绝“永久免费/全年开放”等营业/票价声明。

账本和原始诊断保存在忽略目录 `backend/.demo/guide-finalization-20260922`；最终报告 `report-1790047754607.json`。失败报告 `report-1790046980181.json`、`report-1790047208341.json`、`report-1790047435384.json` 均保留。runner默认dry-run、显式执行、总金额及次数围栏、原账本续用规则见ADR。
