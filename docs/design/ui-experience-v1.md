# UI Experience v1 统一美术规范

2026-09-14 用户更新：沿用本规范的布局、组件与排版，将海洋青品牌主题改为蓝色。当前操作色 `#2563EB`、深蓝文字 `#172F56`、浅面板 `#F1F5FC`。下文 tokens 已同步为当前蓝色规范；成功、警告、风险语义色不改。

状态：2026-09-13 用户认可旅行详情的美术方向，并要求其他界面遵循相同风格。规划、探索、行程、我的和相关二级页面已按本规范实现为完整交互样稿。本规范作为后续新增、重做的用户界面的视觉基线；样稿完成度、生产页面迁移与平台运行验收分别记录。

## 设计语言

蓝白、轻杂志排版、清晰事实层级。规划、探索、航班搜索与比较、行程、收藏和我的等界面共用这套语言。旧 Phase 6 的 near-black 规范保留为既有页面的历史记录，后续视觉改版遵循本规范，按页面逐步迁移。

## Tokens

| 用途 | 值 |
| --- | --- |
| 页面背景 | `#FFFFFF` |
| 主标题/深墨色 | `#172F56` |
| 强调色/操作 | `#2563EB` |
| 浅色面板 | `#F1F5FC` |
| 卡片圆角 | `12–18px`，同类组件保持一致 |
| 主操作圆角 | `16px` |
| 底部弹层顶部圆角 | `22px` |
| 触控最小尺寸 | `44px` |

目的地和主要内容标题使用中文衬线字体；正文与控件使用系统 sans，价格与时刻使用等宽数字。颜色用于层级、状态和来源标记，不用大面积渐变掩盖信息缺口。

## 跨页面一致性

- 共用配色、字号层级、间距规则、线性图标、按钮、导航、标签、卡片与底部弹层；同一种操作在不同页面保持相同外观和反馈。
- 实景图片保持自然色调、明确主体与来源。目的地和探索内容可以用大图；输入、搜索、比较和设置页面按任务需要安排图片与信息密度。
- 白底留白与蓝色主操作贯穿各页，列表、表单和高密度航班信息保持清晰层级。布局随内容调整，避免把旅行详情的首图和段落结构机械复制到所有页面。
- 加载、部分结果、空态、失败、重试、选中、禁用和撤销使用共同的状态表达；来源、估算和未知项始终可见。
- 手机优先，主操作触控区至少 44px；微信与网页保持视觉和交互语义一致，地图、滚动、安全区等由平台适配。

| 页面 | 应用方式 |
| --- | --- |
| 规划与输入 | 清楚的输入层级、浅色分组、蓝色主操作；结果沿用同一套卡片和状态反馈 |
| 探索与目的地 | 实景照片、衬线内容标题、轻杂志排版；沿用相同收藏和详情操作 |
| 航班搜索与比较 | 延续票卡、对齐的时刻和价格、明确中转与来源信息，按比较需要提高密度 |
| 行程与活动详情 | 当前概览、每日行程、航班视图和活动弹层作为已认可的视觉参考 |
| 我的、收藏与设置 | 延续白底、浅色面板、线性图标和列表节奏，按功能组织内容 |

当前共用样式和组件由 `SharedUI.tsx`、`shared.scss`、`VisualMedia.tsx` 与 `experience.scss` 统一维护，避免各页面复制后各自演变。应用通过 `AppExperience.tsx` 连接主导航和二级页面，以下范围已实现为本地交互样稿：

| 页面/入口 | 组件与交互范围 |
| --- | --- |
| 主导航“规划” | `PlannerPage`：想法输入、建议填入、加载/取消/继续、固定示例结果、编辑与重新开始 |
| 主导航“探索” | `ExplorePage`：分类、搜索空态、目的地和灵感详情、来源、收藏、填入规划；目的地抵达信息弹层 |
| 规划中的航班入口 | `FlightExplorer`：表单校验、交换/日期/人数、固定结果、加载/取消/失败、排序、筛选、比较、航段详情、价格提醒 |
| 主导航“行程”与旅行详情 | `TripsPage`、`TripExperience`：列表、归档/恢复、新建；概览、每日安排、航班、活动查看/替换/移出/撤销 |
| 我的收藏、价格提醒 | `CollectionsPage`、`AlertsPage`：分组与空态、打开收藏、取消/撤销；提醒启停、改价、删除/撤销 |
| 主导航“我的” | `ProfilePage`：访客/演示登录、昵称、偏好草稿/保存、字号、隐私、关于、退出与清空收藏 |

既有 `index`、`search`、`route`、`hub-detail` 等注册页面的展示任务在独立应用中有对应界面，早期样稿与后续接入须分开看；生产接入记录见 [UI 清理交接](../HANDOFF_2026-09-13_MAIN_UI_CLEANUP.md)，较新实际能力见 [航班优先验收](../FLIGHT_FIRST_ACCEPTANCE.md)。

## B4 规划中结果展示（当前实现）

正式 Plan 页在等待阶段显示已提交的航班/攻略，攻略标注“已保存，仍在整理/核验”，不暗示 Goal 已满足。已有采用航班与新发布的候选航班分别展示，不能让旧选择遮挡新结果。未选与已选 offer 都可展开只读航段详情。

空草稿在执行期间也可编辑，完成后保留尚未发送的草稿；新的提交、采用和更换航班保持禁用。取消显示等待确认，取消请求失败时继续显示进度与停止未确认提示，确认后才释放提交占用。取消/中断保留已保存成果。账号、auth revision、会话和 Trip 同时约束异步加载和错误回写；被替代的提交不得给新轮写入旧错误。

`node scripts/test-planner-publication.cjs` 运行真实组件代码，通过确定性 hooks 与 Taro host stubs 验证等待分支卡片、草稿输入/保留、取消反馈和航段展开。它不启动浏览器、真实 React renderer 或微信设备；平台滚动、样式、输入法与真实网络表现仍待验收。当前所有验证命令和结果见 [progress](budget-travel-agent/progress.md)。

## 来源资料适用性提示（当前实现）

攻略每日条目、活动详情和 `supportingEvidence` 的研究描述旁统一显示固定的保守提示：“以下为来源资料摘要；其中价格、开放时间和交通时长的当前及出行日适用性尚未核实，请以运营方届时公告为准。” `src/components/artifacts/payload.ts` 是共享 formatter；旧数据缺少 `sourceApplicability`、字段格式错误，或提交了声称“已核验”的任意 notice，均回退到这条固定提示。原始 description 保持不改；同一描述已经包含完整提示时不再重复渲染。

该提示说明来源资料的适用性边界，不改变活动或 supporting evidence 的 `verified`、`partial`、`stale` 核验标签，也不把资料中的参考时长当作行程交通时长。formatter、production presentation 适配和生产/Artifact 展示分别由 `test:artifacts` 与 `test:production-presentation` 的定向用例覆盖；这些是离线源码和确定性组件检查，仍不等同真实 Provider、浏览器像素或微信真机验收。

## B5 展示计时（当前实现）

正式 Plan/PlannerPage 只在本轮已发布引用加载为可用数据、且实际展示分支包含该卡片后，通过 effect 记录首航班/攻略 commit；单纯收到引用、旧已采用航班、空航班结果不计首结果。攻略保存/资料核验状态与整轮 deliveryStatus 分别保留，不能把首卡片当成验收完成。最终 UI 需本轮响应完成且页面退出 busy 后记录。

点击到有效 accepted、首结果和最终 commit 使用同一客户端 performance.now；缺失时钟或自动恢复没有点击则对应值为 null。失败/取消/替代保留已有首结果及独立终态，不补最终完成时间；账号、auth revision、session/request/Trip/conversation 与 turn/generation 约束迟到事件。`src/services/plannerTelemetry.ts` 只保留默认 32 轮的内存快照，未写历史或上传。

证据标签为 `react_effect_commit_not_paint`，不宣称像素已绘制。`test:planner-publication` 同时运行组件、Plan 页面与独立 telemetry sink 回归；真实浏览器/微信 paint、滚动和输入体验仍需 G1 平台验收。详细指标与取数见 [EVALUATION §4.1](budget-travel-agent/EVALUATION.md)。

## 信息结构

### 攻略发布边界（2026-09-21 当前实现）

攻略正文只在服务端发布元数据通过 v1 绑定时展示：`publication.version=1`、`contentContract=limited`、`artifactId` 必须等于当前攻略 artifact，且 `tripContextVersion` 必须等于路线快照版本。预算判断固定显示服务端提供的 `budgetAssessment.notice`，不把 `knownSubtotal: null` 解释成已核算金额。

旧本地缓存、回放或缺少/格式错误的 publication 会关闭攻略自己的标题、摘要、主题、活动说明、notes、warnings 与 supportingEvidence；行程仍保留真实路线天数和航班边界，并显示“旧攻略无法安全展示”的通用提示。Artifact 卡片同样显示通用不可用状态，不回退到原始 payload。历史缓存仅接受带 publication 的攻略；它是客户端输入边界，结构正确本身不证明来源可信，实际来源信任仍依赖服务端 artifact 接口与 owner 范围。

`node scripts/test-production-presentation.cjs` 覆盖缺 publication、publication artifact 身份不一致和路线版本不一致时的关闭行为；`node scripts/test-chat-history.js` 覆盖旧攻略正文注入被丢弃以及有效发布攻略的恢复。两者均为离线确定性检查，不替代真实 API、浏览器或微信设备验收。

研究卡片只显示研究 finding 关联的来源文档标题和经过协议校验的 HTTP(S) URL，并明确显示来源缺失或状态未知；finding 自身的模型标题、摘要和不确定性不进入公共卡片。若本地助手消息关联旧 `travel_guide` 引用但没有可信新发布攻略，恢复时只替换该关联助手消息为静态旧攻略提示，用户消息及其他轮次保持不变。`node scripts/test-artifacts.js` 与 `node scripts/test-chat-history.js` 覆盖这两条边界。

生产攻略 v1 的可选 `budget` 在行程概览和攻略卡单独显示为“全程预算约束”：只接受服务端同时提供的 `amount`、三位大写 `currency`、`scope`、`partyBasis: unspecified`、`period: trip_total`，明确标注为行程总额与同行人数口径未指定，不换算为每日或人均金额。可选 `supportingEvidence` 单独归入“实用与补充信息”，显示服务端恢复的标题、说明、类别、目的地和核验状态；过期的核验记录按 `expiresAt` 显示为资料已过期。它是补充资料，不作为景点安排或用户已选择活动。

固定样例为：上海 → 多哈 → 里斯本，7 天。概览先给路线与总状态；每日行程按天展示城市、安排和来源；航班列表先呈现时刻、价格、航司与中转风险；详情以 bottom sheet 展开完整段落、来源、未知项和操作。

价格示例必须附“示例/估算”语义和来源。未知价格、未核验时刻、图片失败、部分生成应保留空态或警示，不能补猜。地图按照路线顺序讲述节点，并提供文本列表。

## 状态与动作

主要动作保持至少 44px 触控区：局部替换、删除、撤销、收藏。删除后提供可逆撤销；本版收藏仅在预览会话内保留，云端保存及版本冲突属于后续联调。部分结果显示已完成范围与待补内容；图片失败显示占位和重试，不影响文字行程。

跨页收藏和提醒由父应用持有，同一书签与“我的”数量保持一致；提醒只演示设置，不监测票价或通知。二级页面使用返回栈，收藏进入详情、提醒进入航班后返回来源页面，并保留栈内局部状态。普通主导航切换保留规划会话，“新建行程”明确开启空白输入。

偏好编辑与已保存值分开：未保存返回不改变首页摘要，保存后更新本次预览的偏好。昵称、收藏、提醒、偏好和演示登录状态均为当前页面运行态，刷新或重置后清除；界面中的登录展示不获取真实身份。

## 组件策略

`AppExperience` 持有页面栈和跨页演示状态，页面组件负责本地表单、编辑草稿、筛选和详情状态；旅行详情可通过 props 接收固定每日行程。组件不请求业务 API、不解析自然语言或写入业务 stores。规划结果、航班和目的地概览仍使用固定示例，任意输入不会被当作已生成真实结果。

Taro 与 Vite 共享 feature，网页对基础组件、Input/Textarea 事件及地图做平台适配，微信使用独立入口。胜出方案接入真实数据时，再完成展示数据字段映射、来源和 unknown 状态；UI 的视觉完成不决定模型/架构评测结论。

## 早期样稿验证记录（非当前生产验收）

当时扩展后的网页与独立 Taro 类型检查、生产构建均通过。浏览器除原旅行详情的日期、活动、修改与失败状态外，当时还在 390×844、320×740 检查新增页面，实际操作搜索空态、收藏/撤销、跨页返回、航班校验/排序/比较/筛选/详情、提醒增改删撤销、偏好草稿与保存、昵称、字号、隐私、退出和关于。2026-09-20 文档整理没有重新执行这些检查。

以下为早期样稿的历史 QA，不能当作当前生产总状态：地图编号和日程联动已测，但 OSM 在线底图当前网络连接失败，展示降级提示。微信原生地图、SVG、滚动、输入、弹层与安全区尚待设备验收；真实 API、登录、保存与通知未接入、未验收。最终 QA 证据与准确命令继续记录在 [UI 清理历史交接](../HANDOFF_2026-09-13_MAIN_UI_CLEANUP.md)。
# 2026-09-22：终稿语言与展示

正式规划请求显式传当前zh/en locale。新攻略只展示服务端目标语言接纳的名称、简介、理由、主题和overview；未接纳的days为空并显示准备/阻塞状态，不将领域satisfied当作正文ready。保留原文地名，不拼接双语，也不删除字符。

Artifact缓存按id+locale隔离，Plan/详情的请求key包含locale；语言切换后的迟到结果不可替换新语言视图。GET及刷新只读缓存/持久终稿；另一语言首次需求对已接纳终稿发起专用本地化POST，不研究/规划。接纳文本不再经过网页标题替换或每卡追加泛化免责声明，预算未知说明单独保留。媒体以contentVersion+activityId独立扩展，不改正文hash；第三阶段地点地图接入见下节，图片尚未接入。合同见[ADR 0025](../adr/0025-bounded-guide-finalization.md)。

## 2026-09-22 地点与地图

正式概览及每日列表复用同一份地点扩展。只有当前内容 hash 的 resolved 实体能成为标记；孤立 coordinates、城市线索或机场坐标不能充作景点。显式“补全地点”才 POST，挂载/刷新/语言切换仅 GET。概览包含独立城市与已采用航班的可信机场航空示意；每日保留原活动编号，缺失地点不重排，标记打开详情，列表高亮，日期和名称语言同步。街区/街道粒度在详情标注，主题美食没有唯一地点保持未解析。

H5 使用 Leaflet/真实 OSM 瓦片，保留署名，加载失败紧凑降级。微信使用原生桥接；错误/未就绪超时或用户点击收起时回到紧凑提示。原生 updated 无法证明底图瓦片成功，本次模拟器东京底图仍空白，单独保留该平台限制；不以静态背景伪造通过。字段、截图和精确平台边界见[地图验证](budget-travel-agent/PLACES_MAP_2026-09-22.md)。

## 2026-09-22 正式旅行发布展示

正式数据分支沿用现有布局，概览以路线/日期/预算目标/每日主题与紧凑航班状态为主。每日卡片使用接纳名称、时段和简介；详情分为景点简介与结合偏好的推荐理由。无图片使用紧凑提示，无坐标不画虚构地点。zh/en 按当前 locale 提供正文和周围状态/按钮；来源在二级查看保留，取证摘录不进入正文。已知关闭/日期冲突保留短提示。状态与版本、截图及本轮验收边界见 [正式页面报告](budget-travel-agent/PUBLICATION_UI_2026-09-22.md)。旧 Mock 页面和全站视觉风格不属于本次重做范围。
